module.exports = {
  plugins: {
    'postcss-nested': {}, // Enable nested CSS like in your file
    'postcss-attribute-case-insensitive': {}, // Handle attribute selectors with case insensitivity
    autoprefixer: {} // You can also add autoprefixer for better browser support
  }
}
